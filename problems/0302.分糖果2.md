# 模拟
[题目链接](https://kamacoder.com/problempage.php?pid=1382)
## 思路分析
按照题意模拟即可。
## 复杂度分析
1. 时间复杂度：O(1)；
2. 空间复杂度：O(1)。
## 代码实现
### Cpp
``` cpp
#include <bits/stdc++.h>
#define endl '\n'
const int N = 100005, MOD = 1000000007;
using namespace std;
using LL = long long;
using PIL = pair<int, LL>;
using PII = pair<int, int>;
using T3I = tuple<int, int, int>;
int t = 0, dir[]{1, 0, -1, 0, 1};
int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    int candy[5];
    for (int i = 0; i < 5; i++) cin >> candy[i];
    int x = candy[0] / 3;
    candy[0] = x;
    candy[1] += x;
    candy[4] += x;
    for (int i = 1; i < 4; ++i) {
        int x = candy[i] / 3;
        candy[i] = x;
        candy[i - 1] += x;
        candy[i + 1] += x;
    }
    x = candy[4] / 3;
    candy[4] = x;
    candy[3] += x;
    candy[0] += x;
    for (int i = 0; i < 5; i++) cout << candy[i] << " ";
    return 0;
}
```
### Python
``` python
candy = list(map(int, input().split()))
x = candy[0] // 3
candy[0] = x
candy[1] += x
candy[4] += x
for i in range(1, 4):
    x = candy[i] // 3
    candy[i] = x
    candy[i - 1] += x
    candy[i + 1] += x
x = candy[4] // 3
candy[4] = x
candy[3] += x
candy[0] += x
print(' '.join(map(str, candy)))
```
### Java
``` java
import java.io.*;
import java.util.*;
public class Main {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		int[] candy = new int[5];
		for (int i = 0; i < 5; i++) {
			candy[i] = Integer.parseInt(st.nextToken());
		}
		int x = candy[0] / 3;
		candy[0] = x;
		candy[1] += x;
		candy[4] += x;
		for (int i = 1; i < 4; ++i) {
			x = candy[i] / 3;
			candy[i] = x;
			candy[i - 1] += x;
			candy[i + 1] += x;
		}
		x = candy[4] / 3;
		candy[4] = x;
		candy[3] += x;
		candy[0] += x;
		StringBuilder sb = new StringBuilder();
		for (int i = 0; i < 5; i++) {
			sb.append(candy[i]).append(" ");
		}
		System.out.println(sb.toString().trim());
	}
}
```
### Go
``` go
package main
import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	reader := bufio.NewReader(os.Stdin)
	writer := bufio.NewWriter(os.Stdout)
	defer writer.Flush()
	candy := make([]int, 5)
	for i := 0; i < 5; i++ {
		fmt.Fscan(reader, &candy[i])
	}
	x := candy[0] / 3
	candy[0] = x
	candy[1] += x
	candy[4] += x
	for i := 1; i < 4; i++ {
		x = candy[i] / 3
		candy[i] = x
		candy[i-1] += x
		candy[i+1] += x
	}
	x = candy[4] / 3
	candy[4] = x
	candy[3] += x
	candy[0] += x
	for i := 0; i < 5; i++ {
		fmt.Fprint(writer, candy[i], " ")
	}
}
```